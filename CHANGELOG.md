# 更新记录（Change Log）

> 本文件记录话费代充系统「用户端」可交互 HTML 原型的每次更新。
> 约定：每次修改代码后提交 GitHub，并在本文件追加一条记录。

---

## 2026-08-31（v1.2.1 / 迭代 88）修复客服悬浮按钮折叠态图标未居中

- **问题（components/ui/service-qr-float.tsx）**：折叠态按钮用 `gap-2` + `px-4`，即使「在线客服」文字收起为 `max-w-0`，flex 仍在图标右侧保留 8px 间隙，把图标挤到圆形按钮中心的左侧，静止/未悬停时看起来不对齐。
- **修复**：间隙改为默认 `gap-0`、仅悬停时 `group-hover:gap-2`；左右内边距由 `px-4` 收为 `px-3.5`，使折叠态成为规整的 50×50 圆形按钮。实测图标中心距左右边缘均为 25.0px，完全居中；悬停展开「在线客服」的交互不变。
- 版本号 `package.json` 由 `1.2.0` 升至 `1.2.1`。

## 2026-08-31（v1.2.0 / 迭代 87）首页增加企业微信客服悬浮二维码

- **新增组件（components/ui/service-qr-float.tsx）**：右下角 `fixed bottom-6 right-6 z-40` 常驻悬浮客服入口。折叠态为橙色渐变圆形按钮（`Headphones` 图标，悬停展开「在线客服」文字）；展开态在按钮上方弹出扫码面板——橙色渐变标题栏「企业微信客服」+ 关闭按钮，内嵌 `FakeQr`（复用现有仿真二维码组件，seed=`fansheng-wecom-service`）与「微信扫码，添加专属客服 / 7×24 小时在线 · 充值 / 开票 / 售后」文案。
- **交互**：点击按钮 toggle 展开/收起，点击面板右上角 `X` 或鼠标移出容器（`onMouseLeave`）收起；面板用 `opacity/scale/translate` 过渡淡入，`origin-bottom-right` 从按钮处展开。
- **挂载（app/page.tsx）**：在 `AppShell` 内页脚之后引入 `<ServiceQrFloat />`，仅首页展示。
- **验证**：桌面 1153px 与移动 390px 下按钮与展开面板均正常，二维码清晰、面板不溢出视口；`tsc --noEmit` 通过。
- 版本号 `package.json` 由 `1.1.9` 升至 `1.2.0`。

---

## 2026-08-31（v1.1.9 / 迭代 86）登录/注册页左上角 LOGO 底色改为纯白 #ffffff

- **登录页（app/login/page.tsx）与注册页（app/register/page.tsx）**：左上角 LOGO 外层链接的胶囊底色由 `bg-white/90 backdrop-blur`（90% 半透明白 + 背景模糊，橙色底会透出）改为纯白 `bg-white`（#ffffff），并移除 `backdrop-blur`。
- **验证**：登录页胶囊实测 `background-color: rgb(255, 255, 255)`，LOGO 落在纯白底上、无橙色透色。
- 版本号 `package.json` 由 `1.1.8` 升至 `1.1.9`。

---

## 2026-08-31（v1.1.8 / 迭代 85）更换品牌 LOGO 为新版横向锁定形

- **替换素材（public/images/logo-fansheng.png）**：由旧的 600×600 方形图换为新版 1998×730 横向锁定形（低多边形地球图标 + 梵胜通信 + Fansheng Communication）。
- **组件适配（components/ui/brand-logo.tsx）**：用 canvas 逐像素测得新图内容边界为 x 123–1879 / y 112–624，内容比例 3.43:1，四周留白约左右 6% / 上下 15%（基本对称）。据此把展示盒由 `h-9 w-[188px] scale-[1.9]` 调整为 `h-9 w-[124px] scale-[1.43]`，`object-contain object-center` 放大裁掉内置留白，使锁定形填满且清晰、无变形。
- **验证**：首页顶栏、登录 / 注册页浅色胶囊、390px 移动端首页均正常呈现，图标与中英文两行文字清晰居中、无溢出。
- 版本号 `package.json` 由 `1.1.7` 升至 `1.1.8`。

---

## 2026-08-26（v1.1.7 / 迭代 84）订单管理 H5 导出与重置按钮宽度对齐账户中心

- **订单列表查询 / 重置等宽（app/orders/page.tsx）**：两个按钮原为内容宽度（H5 下 92px / 93px，右侧留大片空白），加上 `max-[460px]:flex-1` 后在窄屏平分整行（390px 下 144px / 146px），与账户中心 `.filter-actions button { flex: 1 }` 的 460px 断点保持一致；460px 以上仍是自动宽度左对齐，桌面端不变。
- **订单详情导出按钮宽度（app/order-detail/page.tsx）**：`导出数据` 原为 `w-full`，H5 下独占整行 316px，与同组 152px 的查询 / 重置明显不齐。改为 `w-[calc(50%-5px)]`（即 `(100% - gap 10px) / 2`，与 `flex-1` 按钮算出的宽度完全相同），390px 下三个按钮实测 152 / 154 / 153，导出按钮整齐落在查询下方。
- **平板区间收敛**：新增 `sm:w-auto sm:self-start lg:self-auto`，使 640–1023px 区间的导出按钮由原先被拉伸的 726px 改为内容宽度 105px 左对齐；`lg` 及以上恢复 `self-auto`，保持原先「筛选项在左、导出数据靠右」的桌面布局与垂直居中不变。
- 已验证 360 / 390 / 800 / 1153px 四个断点均无溢出，导出 CSV 功能正常（toast 正常弹出）。
- 版本号 `package.json` 由 `1.1.6` 升至 `1.1.7`。

---

## 2026-08-26（v1.1.6 / 迭代 83）代理商中心佣金明细筛选增加查询与重置按钮

- **交互模型对齐账户中心与订单中心（app/agent/page.tsx）**：佣金明细原先输入即实时过滤，改为「暂存 + 提交」两段式——新增 `CommFilters` 类型与 `EMPTY_COMM_FILTERS` 常量，`commForm` 保存正在编辑的条件、`commApplied` 保存已生效的条件；移除 `consId` / `consMember` / `consFrom` / `consTo` 四个独立 state，`filteredComm` 的 `useMemo` 依赖收敛为仅 `commApplied`。
- **查询 / 重置按钮**：筛选栏末尾新增 `.filter-actions`，放入 `查询`（primary + `Search` 图标）与 `重置`（`variant="outline"` + `RotateCcw` 图标），复用账户中心 v1.1.5 已建立的样式，本次无需新增 CSS。
- **`applyCommFilter` / `resetCommFilter`**：点「查询」提交 `commForm` 到 `commApplied` 并回到第 1 页；点「重置」同时清空两者并回到第 1 页。原先散落在各输入框 `onChange` 里的 `setCommPage(1)` 副作用一并移除。
- **单号 / 来源用户框支持回车查询**：`onKeyDown` 监听 Enter 触发 `applyCommFilter`，并加入中日韩输入法保护（`isComposing` 与 Safari `keyCode === 229`），避免拼音选字时误提交。
- 版本号 `package.json` 由 `1.1.5` 升至 `1.1.6`。

---

## 2026-08-26（v1.1.5 / 迭代 82）账户中心筛选参考订单中心，增加查询与重置按钮

- **交互模型对齐订单中心（app/account/page.tsx）**：原先输入即实时过滤，改为「暂存 + 提交」两段式——新增 `FlowFilters` 类型与 `EMPTY_FILTERS` 常量，`form` 保存正在编辑的条件、`applied` 保存已生效的条件，`useMemo` 过滤依赖由 5 个独立 state 改为仅依赖 `applied`。
- **查询 / 重置按钮**：筛选栏末尾新增 `.filter-actions`，放入 `查询`（`Button` 默认 primary + `Search` 图标）与 `重置`（`variant="outline"` + `RotateCcw` 图标），与订单中心用法、图标、文案完全一致。
- **`applyFilter` / `resetFilter`**：点「查询」将 `form` 提交给 `applied` 并回到第 1 页；点「重置」同时清空 `form` 与 `applied` 并回到第 1 页。移除原 `resetPage()` 高阶函数。
- **关键词框支持回车查询**：`onKeyDown` 监听 Enter 触发 `applyFilter`，并加入中日韩输入法保护（`isComposing` 与 Safari `keyCode === 229`），避免拼音选字时误提交。
- **按钮样式（app/globals.css `.filter-actions`）**：`grid-column: 1 / -1` 独占一整行，`display:flex` + `gap:10px` 左对齐，按钮高度统一 40px 与输入框齐平；`≤460px` 断点下两按钮 `flex:1` 平分整行宽度。
- 版本号 `package.json` 由 `1.1.4` 升至 `1.1.5`。

---

## 2026-08-26（v1.1.4 / 迭代 81）账户中心筛选项栅格化，修复长短不齐与溢出背景块

- **筛选栏（app/globals.css `.flow-filter-bar`）**：由 `flex + flex-wrap` 改为 `grid`，`grid-template-columns: repeat(auto-fit, minmax(190px, 1fr))`，各筛选项等宽对齐，不再「长的长短的短」。
- **输入控件（`.filter-field .field-input`）**：移除 `min-width:150px`，改为 `width:100%; min-width:0; max-width:100%`，并给 `.filter-field` 加 `min-width:0`，彻底消除超出面板背景色块的溢出。
- **下拉框（`.filter-field select.field-input`）**：`appearance:none` 去掉浏览��原生箭头（各浏览器宽度不一，也是观感别扭来源），改用内联 SVG 箭头 + `padding-right:34px`，与输入框视觉统一。
- **日期区间（`.filter-field-date` / `.date-range`）**：新增 `.filter-field-date` 类占两列；`.date-range` 改为 `grid-template-columns: minmax(0,1fr) auto minmax(0,1fr)`，两个日期框等宽且不溢出。
- **响应式断点**：`≤760px` 两列（日期区间跨两列）、`≤460px` 单列（日期区间仍左右并排，字号与内边距收紧）。
- **代理商中心（app/agent/page.tsx）**：佣金明细筛选栏移除内联 `flex:1 / minWidth` 覆盖，日期字段改用 `.filter-field-date`，与账户中心口径一致。
- 版本号 `package.json` 由 `1.1.3` 升至 `1.1.4`。

---

## 2026-08-17（迭代 80）顶栏语音回收文字颜色与其它导航一致

- **顶部导航（assets/css/style.css `.nav-todo`）**：原 `.nav a.nav-todo` / `.icon-nav-item.nav-todo` 用 `opacity:.5/.45` 淡化，导致「语音回收」文字颜色比其它导航更浅。改为 `opacity:1` 且 `color:var(--text-2)`（与其它导航项一致），hover 仍高亮为 `var(--primary)`；点击提示「语音回收功能待开放」行为不变。

---

## 2026-08-14（迭代 79）订单详情基本信息增加优惠折扣显示

- **订单详情页（assets/js/order-detail.js）**：话费充值与��信群发两类订单的「基本信息」网格新增三条折扣字段，与确认订单弹窗口径一致：
  - 优惠折扣：`DB.DISCOUNT.label`（9.9折）
  - 优惠金额：原价 ×（1 - 0.99），红色负值
  - 实付总额：原价 × 0.99
- 充值单按 `unit*count` 原价计算；短信单按 `o.total` 原价计算，均套用 `DB.DISCOUNT`。

---

## 2026-08-14（迭代 78）代理申请区分企业/个人 + 提现结算说明 + 语音回收入�� + 折扣字段

- **代理申请表单（agent.html）**：新增「用户类型」分段切换（企业用户 / 个人用户）。
  - 企业用户：表单不变，保留营业执照上传。
  - 个人用户：营业执照字段替换为「身份证正面」「身份证反面」两个图片上传字段（`handleIdFrontFile` / `handleIdBackFile`），提交时校验两证必传。
  - 驳回后「重新申请」会复原用户类型与对应资质上传状态。
- **提现申请弹窗（agent.html `#withdrawMask`）**：新增「每月结算时间」说明（每月 25 日为结算日，次月 5 日前审核打款，遇节假日顺延）。
- **顶部导航栏（app.js `NAV_ITEMS`）**：在「短信群发」之后新增「语音回收」入口（`key:voiceback`），点击 `App.toast('语音回收功能待开放')` 提示；样式以 `.nav-todo` 弱化。
- **话费充值 / 短信群发底栏与确认弹窗（recharge.html / sms.html）**：新增「折扣」统计字段（数据来源 `DB.DISCOUNT = { rate:0.99, label:'9.9折' }`）；确认提交订单弹窗同步展示「折扣 / 优惠金额 / 实付总额（9.9折）」，余额卡���「本次消费金额 / 消费后余额」改用折扣后应付金额。

---

## 2026-08-14（迭代 77��修正提现相关弹窗左右留白归属

- **问题**：迭代 73 把「提现弹窗表单左右顶边贴边去留白」的 CSS 误加到了 **佣金提现弹窗 `#withdrawMask`**，而实际需求是作用于 **提现记录弹窗 `#wdRecordMask`**。导致佣金提现弹窗内容被压到左右贴边（无留白），提现记录表格却保留了 24px 左右内边距。
- **修复**：
  - 移除 `#withdrawMask .modal > *:not(h3){padding-left:0;padding-right:0}` / `#withdrawMask .apply-form{margin-top:0}` / `#withdrawMask .wd-limit-bar{margin-bottom:0}` 三条贴边规则，佣金提现弹窗恢复全局 `.modal > *` 默认 24px 左右留白。
  - 把「左右贴边」规则迁移到 `#wdRecordMask .modal > div[style*="overflow"]`，在原有 `padding-top:0` 基础上新增 `padding-left:0; padding-right:0`，使提现记录表格紧贴标题与左右边、无顶部/左右留白。
- 校验：agent.css 已改；确认无 `#withdrawMask .modal > *` 残留贴边规则。

---

## 2026-08-13（迭代 76）编辑弹窗验证码输入框与按钮同行

- **需求**：修改密码弹窗内「短信验证码」输入框需与「获取验证码」按钮处于**同一行**。
- **根因**：`.code-row` 的 flex 横向布局规则原本只写在 `agent.css`（仅 `agent.html` 加载）。迭代 75 将编辑弹窗���为 `app.js` 全局注入后，在 话费充值 / 短信群发 / 订单管理 / 账户中心 四页（不加载 `agent.css`）中 `.code-row` 无样式，输入框与按钮退化为竖向堆叠。
- **修复**：在全局 `style.css`（所有页面均加载）新增作用域限定的规则，仅对「修改密码弹窗」这类 `.apply-form` 内的 `.code-row` 生效，避免误伤 `register.html` / `login.html` 顶层的 `.code-row`（其结构为嵌套 `.field`）：
  - `.apply-form .code-row { display: flex; gap: 8px; align-items: stretch; }`
  - `.apply-form .code-row input { flex: 1; min-width: 0; width: auto; }`
  - `.apply-form .code-row .btn { flex-shrink: 0; white-space: nowrap; }`
  - 编辑弹窗 HTML 本就以 `.apply-form` ��裹，故以上规则在 5 个受保护页面通用；与 `agent.css` 既有 `.code-row` 规则不冲突（agent 页同时命中，效果一致）。
- 校验：app.js 未改动；确认 `style.css` 已新增 `.apply-form .code-row` 三段规则。

---

## 2026-08-13（迭代 75）编辑弹窗全局化 + 改密后退出登录

- **1. 编辑（修改密码）弹窗全局化，5 个受保护页面均可打开**：
  - 将原先仅在 `agent.html` 内的 `#editPwdMask` 弹窗 HTML 及其 5 个 JS 函数（`openEditPwdModal` / `closeEditPwdModal` / `sendEditPwdCode` / `resetEditPwdCodeBtn` / `doEditPwdSubmit`）、两个变量（`editPwdSentCode` / `editPwdCodeTimer`）**整体迁移到 `app.js`**。
  - `app.js` 新增 `EDIT_PWD_HTML` 常量，并在 `DOMContentLoaded`（或脚本已就绪时立即）通过 `injectEditPwdModal()` 把弹窗注入 `document.body`，注入前判断 `#editPwdMask` 是否已存在，避免重复注入。
  - `renderTopbar()` 本就向所有受保护页面注入「编辑」按钮并绑定 `openEditPwdModal()`，而 `requireLogin()` 在 话费充值 / 短信群发 / 订单管理 / 账户中心 / 代理商中心 5 个页面均被调用 → 编辑按钮与修改密码弹窗现在**5 个页面通用**。
  - 因 `app.js` 为 IIFE，弹窗内联 `onclick`（`open/closeEditPwdModal`、`sendEditPwdCode`、`doEditPwdSubmit`）需显式挂到 `window` 供 HTML 解析器调用，已在 IIFE 末尾统一 `window.xxx = xxx`。
- **2. 密码修改成功后退出登录，需重新登录**：
  - `doEditPwdSubmit()` 在校验全部通过后 `closeEditPwdModal()` → toast「密码修改成功，正在退出登录…」→ 1200ms 后 `logout()` 清除 `hc_session` 并 `location.href="login.html"`，强制重新登录。
- 校验：agent.html 已无 `editPwd*` 残留引用（Grep 0 命中）；app.js / data.js `node --check` 通过；5 个页面均经 `requireLogin` → 编辑按钮与弹窗可正常触发。

---

## 2026-08-13（迭代 74）提现记录驳回原因列宽修复（避免 ~4 字换行）

- **根因**：`wd-table` 的「提现状态」列（第 4 列）无 `min-width` 约束，表格布局算法将空间优先分配给其他列，导致驳回原因文字在极窄宽度下 ~4 个字符就强制换行，样式错乱。
- **修复**：
  - 第 4 列（`th:nth-child(4)` / `td:nth-child(4)`）新增 `min-width:120px`，保证驳回原因文本有足够展示空间。
  - `.wd-reason` 补充 `white-space:normal; word-break:break-all`，确保长文本按词/字正常断行而非异常截断。
- 本地提交 `f5180b5`（1 文件，+8/-1）。⚠️ 推送失败（同前）。

---

## 2026-08-13（迭代 73）佣金筛选拆分 + 提现弹窗贴边

- **1. 佣金明细筛选「单号 / 来源用户」拆分为两个独立筛选项**：
  - 筛选栏原单一「单号 / 来源用户」输入框（`consKw`，同时匹配 id 与 member）拆成 **单号**（`consId`）与 **来源用户**（`consMember`）两个独立 `filter-field`。
  - `getFilteredCommissions()` 同步改为分别取 `consId` / `consMember` 值，各自独立过滤（单号查 `c.id`、来源用户查 `c.member`），保留产生时间范围筛选；空值该条件不参与过滤。
- **2. 提现弹窗表单左右顶边去除留白**：
  - 新增 CSS：`.wd-limit-bar` 上方说明与限额条、表单区通过 `#withdrawMask .modal > *:not(h3) { padding-left:0; padding-right:0 }` 取消全局 `.modal > *` 的 24px 左右内边距，使表单字段**贴边左右**显示。
  - 移除提现表单 `.apply-form` 内联 `margin-top:14px`（HTML 改为无 margin），并加 `#withdrawMask .apply-form { margin-top:0 }` / `#withdrawMask .wd-limit-bar { margin-bottom:0 }`，使表单**紧贴顶边**（限额条下方无额外间距）。
  - 标题栏（`h3`）保留原有内边距样式不变。
- 校验：agent.html 内联脚本（2 段）new Function 通过；data.js / app.js node --check 通过；确认无 `consKw` 残留引用。

---

## 2026-08-13（迭代 72）去掉佣金金额筛选 + 顶栏编辑按钮+修改密码弹窗 + 提现记录样式修复

- **1. 佣金明细筛选项去掉「佣金金额」**：删除筛选栏中的 `consAmt` ��入框（`filter-field` 容器 + label + input）；同步清理 `getFilteredCommissions()` 中 `amt`/`amtStr`/`isNaN(amt)` 相关过滤逻辑；保留 单号/来源用户关键词 + 产生时间范围 两项筛选。
- **2. 顶栏增加「编辑」按钮 + 修改密码弹窗**：
  - `app.js` 的 `renderTopbar()` 在账号��息（`s.name/s.account`）与「退出」按钮之间插入 `<button id="btnEditPwd">编辑</button>`（`btn-ghost btn-sm mobile-hide`），并绑定 click 事件调用全局 `openEditPwdModal()`。
  - `agent.html` 新增修改密码弹���（`#editPwdMask`）：表单含 绑定手机号（预填 `s.phone`）/ 短信验证码（获取验证码 60s 倒计时，演示码 111111）/ 新密码（6-20 位）/ 确认新密码 / 取消 + 确认修改 双按钮。
  - JS 函数：`openEditPwdModal()`（预填手机号、重置表单和验证码 UI）、`closeEditPwdModal()`、`sendEditPwdCode()`、`resetEditPwdCodeBtn()`、`doEditPwdSubmit()`（校验手机号格式 → 是否已发码 → 验证码匹配 → 密码长度 6-20 → 两次一致 → toast 成功并关闭弹窗）。
- **3. 提现记录弹窗样式修复**：
  - 「查看凭证」按钮改为 `display:inline-flex; white-space:nowrap`，确保文字**横向排列**不竖排；打款凭证列（第 5 列）设 `min-width:80px; text-align:center` 给足宽度。
  - 弹窗内容区（`overflow-x:auto` 容器）添加 `padding-top:0 !important; margin-top:-4px` 消除标题与表格之��的**顶部留白**，对齐截图标准。
- 校验：agent.html 内联脚本（2 段）new Function 通过；app.js / data.js node --check 通过；确认无 `consAmt` 残留引用。

---

## 2026-08-13（迭代 71）佣金明细扩至 25 条 + 分页 20 + 提现记录增加打款凭证列

- **1. 佣金明细列表数据增加至 25 条 + 分页 20 条/页**：
  - `data.js` 的 `COMMISSIONS` 由原先按真实企业下线计算的 4 条，改为**显式生成 25 条**（直推 13 条 + 间推 12 条，含真实下线 李娜/刘洋/吴昊/蒋涛，其余为演示企业客户）；编号 `C2026D001`~`C2026D013`、`C2026I001`~`C2026I012`，金额按 直推 3‰ / 间推 2‰ 实时计算，ID 唯一。
  - 作废原先基于 `#commBody` 行 `data-*` 属性的 DOM 显隐筛选（`applyConsFilter` 改为数据驱动），新增 `getFilteredCommissions()`（按 单号/来源用户关键词 + 金额 + 时间范围 过滤）与 `renderCommList()`。
  - 新增分页状态 `commPageIndex` / `COMM_PAGE_SIZE = 20`，列表下方新增分页容器 `<div class="pagination" id="commPager">`；`renderCommPager()` / `gotoCommPage()` 复用既有 `.pagination` 样式；筛选时自动回到第 1 页（25 条 → 2 页：20 + 5）。
- **2. 提现记录弹窗新增「打款凭证」列**：
  - `wd-table` 表头在原 5 列基础��新增 **打款凭证** 列（位于「提现状态」之后、「提现时间」之前）。
  - 渲染逻辑（`renderWdRecords`）：仅 `已到账（打款成功）且含 voucher` 的行显示「查看凭证」按钮（点击 `openVoucher` 弹 SVG 凭证）；其余状态（待审核/待打款/审核驳回）以灰���「—」占位。
  - 同步将原本嵌在「提现状态」单元格内的凭证按钮**移出**，避免与状态/驳回原因混排；新增 `.wd-dash` 灰色占位样式。
- 校验：`agent.html` 内联脚本（2 段）经 `new Function` 解析通过；`data.js` 经 `node --check` 与整文件加载验证，`COMMISSIONS.length = 25`（直推 13 / 间推 12，ID 全唯一）；确认无 `data-amt/data-member/data-id` 残留引用。

---

## 2026-08-13（迭代 70）我的团队计数入 TAB + 提现记录分页与样式优化

- **1. 我的团队弹窗：数据直接显示在 TAB 内（去掉独立统计条）**：
  - 删除独立的「数据统计条」`.team-stats`（HTML `<div id="teamStats">` + `renderTeamStats()` 函数 + 调用），相关数据改为直接写进 TAB 标签文案：`直推（7）` `间推（8）`（`DB.AGENT_DIRECT.length` / `DB.AGENT_INDIRECT.length`）。
  - TAB 标签内新增 `<span id="directCount">` / `<span id="indirectCount">`，在 `switchTeamTab()` 中实���赋值。
  - 保留原有的**每页 10 条分页**机制（`teamPager` / `renderTeamPager()` / `gotoTeamPage()` / `TEAM_PAGE_SIZE = 10`）不变。
  - 原 `teamSub` 描述文案精简为「由您直接推广注册的下级客户」/「由您的直推客户再次推广注册的下级客户」（去掉重复的「共 N 人」）。
  - 同步删除 `agent.css` 中已无用的 `.team-stats / .ts-item` 样式。
- **2. 提现记录弹窗：列表样式优化 + 增加分页**：
  - 新增分页状态 `wdPageIndex` / `WD_PAGE_SIZE = 10`，并在弹窗底部新增分页容器 `<div class="pagination" id="wdPager">`。
  - 重写 `renderWdRecords()`：按 `WD_PAGE_SIZE` 切片渲染，`openWdRecordModal()` 打开时重置 `wdPageIndex = 1`；新增 `renderWdPager()`（上一页/下一页 + 「第 X / Y 页（共 N 条）」）与 `gotoWdPage(p)`。
  - 表格样式优化（`.wd-table`）：表头浅灰底、单元格 11px/13px 内边距、偶数行浅底、金额加粗绿色、单号/时间 `nowrap` 防折行、状态徽标与凭证按钮间距，驳回原因换行展示。
- 校验：agent.html 内联脚本（24881 字符）经 `new Function` 解析通过；确认无 `renderTeamStats / teamStats / ts-item` 残留引用。

---

## 2026-08-13（迭代 69）代理中心六项调整（筛选样式/团队TAB/推广标题/提现验证码/记录凭证/去账户面板）

- **1. 筛选项样式错乱修���**：根因为佣金明细筛选栏 `.flow-filter-bar / .filter-field / .date-range` 的样式仅定义在 `account.css`，而 `agent.html` 并未加载该样式表，导致筛选栏无样式、布局错乱。已在 `agent.css` 中补全**自包含**的筛选栏样式（flex 布局 + 输入框 36px 高 + 日期区间 `.date-range`），代理页面不再依赖 account.css。
- **2. 我的团队弹窗 TAB 调整 + 数据统计 + 分页**：
  - TAB 文案去掉「TAB」字眼，改为 **直推 / 间推**。
  - 新增**数据统计条**（`.team-stats`）：展示 `直推（7）` `间推（8）`，数据来自 `DB.AGENT_DIRECT / DB.AGENT_INDIRECT` 实时长度。
  - 列表**分页，每页 10 条**（`TEAM_PAGE_SIZE = 10`）；新增 `renderTeamPager()` 生成上一页/下一页 + 「第 X / Y 页（共 N 条）」，切换 TAB 自动回到第 1 页。
- **3. 推广链接弹窗标题对齐**：移除 `promoModal` 标题 `h3` 上的 `justify-content:center` 内联样式，改由全局 `.modal > h3`（flex + space-between）统一管理，实现**标题左对齐、关闭 ICON 右对齐**。
- **4. 佣金提现表单增加手机验证码**：新增「绑定手机号」输入框（打开弹窗时预填 `s.phone`）与「短信验证码」输入 + 「获取验证码」按钮（60s 倒计时，演示验证码通过 toast 展示）；`doWithdrawSubmit()` 提交前校验手机号格式、是否已获取验���码、验证码是否正确。
- **5. 提现记录增加状态与凭证**：
  - 状态扩展为 **打款成功（已到账）/ 待审核 / 待打款 / 审核驳回** 四种；驳回行显示**驳回原因**（`.wd-reason`）。
  - 打款成功行提供「**查看凭证**」按钮，弹窗以 SVG 绘制转账凭证图（金额 / 收款账户 / 单号 / 时间 / 已到账）。
  - `data.js` 的 `WITHDRAWS` 增加 `voucher` 字段（标记可看凭证）并新增「待打款」「审核驳回（含 rejectReason）」两条演示数据。
- **6. 代理中心去掉底部提现账户显示**：移除页面底部「提现账户」面板（`#wdAccountPanel` 及 `renderWdAccount()` 函数与调用）；保留提现表单提交后 `s.wdAccount` 写入（下次打开预填银行卡信息）的逻辑。
- 校验：agent.html / data.js 内联与文件级 JS 经 `new Function` / `node --check` 解析通过；确认无 `renderWdAccount / wdAccountPanel / 直推 TAB / 间推 TAB` 残留引用。

---

## 2026-08-13（迭代 68）代理中心：卡片一行显示、功能入口下移、合并双列表

- **卡片数据改为一行显示不换行**：`agent.css` 为 `#agentTiles` 增加专属规则 `grid-template-columns: repeat(5, minmax(0,1fr))`，覆盖全局 `.tiles` 的 4 列布局，使 5 张卡片（我的团队 / 累计消费金额 / 累计获得佣金 / 可提现佣金 / 已提现���金）始终排成一行；卡片内数字与标签加 `white-space:nowrap` 防挤压换行；窄屏（≤635px）改为 `minmax(96px,1fr)` 横向滚动而非折行。
- **功能入口下移**：「我的团队 / 推广链接 / 佣金提现 / 提现记录」四个操作按钮行（`.op-btn-row`）调整至卡片数据 `#agentTiles` 正下方，与卡片区视觉分离、符合「先数据后操作」的阅读顺序。
- **合并两个相似列表（解决用户困惑）**：原「可通明细」与「佣金明细」两个表格结构相似、易混淆，本次删除「可通明细」面板，仅保留「佣金明细」单一列表，其筛选栏整合原两套筛选条件（单号/来源用户关键词 + 佣金金额 + 产生时间范围）。
  - 同步修复 JS 不一致：`renderAgentCenter()` 移除了对已删除 `consBody` 的 `renderConsDetails()` 调用；`applyConsFilter()` 改为筛选 `#commBody tr`，并为 `commBody` 行补充 `data-id`/`data-member`/`data-amt`/`data-time` 属性，使合并后的单一列表筛选仍可正常工作；删除已无用的 `renderConsDetails()` 函数。
- `data.js` 中 `CONSUMPTION_DETAILS` 数据保留（不再被代理中心引用，无副作用）。

---

## 2026-08-13（迭代 67）代理商中心页面按需求规格全面重构

- **代理商中心（已通过状态）全面重构**，按需求规格图重新布局：
  - **页面标题**：改为「代理中心」，说明文案更新为「用户申请升级为代理���，解锁推广链生成、下级（二级分销）管理、佣金明细查看与提现等分佣能力」。
  - **卡片数据（5 张）**：我的团队（直推+间推总人数，可点击进入团队弹窗）、累计消费金额、累计获得佣金、可提现佣金、已提现佣金。
  - **可通明细（新增）**：筛选栏（单号/用户名关键词 + 消费金额 + 消费时间范围）+ 表格（单号 / 用户名 / 消费金额 / 消费时间），支持实时筛选。数据来源 `DB.CONSUMPTION_DETAILS`。
  - **佣金明细**：列调整为 单号 / 来源用户 / 推荐类型 / 佣金金额 / 产生时间（移除冗余的「成员类型」列）。
  - **操作按钮行（4 个按钮）**：我的团队 / 推广链接 / 佣金提现 / 提现记录 —— 均以弹窗形式打开对应功能模块。
  - **我的团队弹窗**：改为 TAB 切换（直推 TAB / 间推 TAB）；列表列调整为 昵称 / 手机号 / 推荐类型 / 上级用户 / 获得佣金 / 注册时间（对齐需求规格）。
  - **推广链接弹窗**：居中展示专属二维码 + 专属推广链接 + 复制二维码/复制链接双按钮；提示文案「客户通过此链接/二维码注册将永久绑定为您名下的下级成员」。
  - **佣金提现弹窗**：顶部展示限额信息条（可提现/最低 ¥100 / 手续费 0% / 单笔最高 ¥50,000 / 日累计最高 ¥200,000）；表单字段（提现金额 / 开户银行 / 户名 / 银行卡号）；提交后自动记录提现账户到 session（下次打开预填）；校验含最低/最高限额。
  - **提现记录弹窗**：表格列 提现单号 / 提现金额 / 提现账户（银行+户名）/ 提现状态（待审核/打款成功/审核驳回+原因）/ 提现时间；新增一条「待审核」演示数据。
  - **提现账户展示（新增）**：页面底部展示当前记录的提现账户（户名 / 银行名称 / 收款账号），首次未填写时显示提示文案。
- **data.js 新增 `CONSUMPTION_DETAILS`**（8 条消费明细数据）；`WITHDRAWS` 新增 `accountName` 字段和一条「待审核」记录。
- **agent.css 大幅扩展**：新增操作按钮行 `.op-btn-row`、团队 TAB `.team-tabs/.team-tab`、提现限额条 `.wd-limit-bar`、推广链接弹窗 `.promo-modal`、佣金 pill 标签 `.comm-pill`、响应式适配。

---

## 2026-08-13（迭代 66）代理申请：驳回回填 + 修复状态叠加显示

- **问题 1 · 驳回后「重新申请」回填表单**：`reapply()` 不再清空字段，改为从 `s.agentApplyData`（提交时保存的真实姓名/联系电话/联系邮箱）回填三字段，并恢复协议勾选，方便用户修改后直接重提；营业执照上传区因浏览器安全限制无法程序回填，恢复默认态。
- **问题 2 · 修复「表单 + 驳回状态」叠加**：根因为 `showReviewStatus()` 只显示审核面板，未隐藏申请表单面板（`applyPanel` 默认可见）。切换页面再回来时 `initAgentPage()` 读到 `agentStatus="rejected"` 调用 `showReviewStatus` 后，表单与驳回卡片同时显示。修复：在 `showReviewStatus()` 开头统一隐藏 `applyPanel` 与 `agentMain`，确保只展示审核状态区。
- 提交 `s.agentApplyData` 保留用于回填；`agentStatus`/`agentRejectReason`/`isAgent` 在回到表单时清理。

---

## 2026-08-13（迭代 65）代理申请表单字段标题去提示

- **agent.html 申请表单**：移除「真实姓名」「联系电话」「联系邮箱」三个字段标题后的括号文字提示（分别为「与身份证一致」「用于接收审核通知」「用于接收合同电子件」），字段标题仅保留纯名称。营业执照字段提示保留不变。
- 纯展示层微调，逻辑与校验不受影响。

---

## 2026-08-13（迭代 64）代理申请页面按需求规格全面重构

- **agent.html 全面重写**：从简单的确认对话框升级为完整的代理申请流程。
  - **申请表单**：真实姓名、联系电话、联系邮箱、营业执照上传区（原型模拟）、协议勾选、提交按钮。表单校验（姓名非空、手机号格式、邮箱格式、协议必勾）。
  - **代理商合作协议弹窗**：点击《代理商合作协议》弹出完整协议内容（总则/合作模式/权利义务/佣金结算/协议终止），底部「我已阅读并同意」按钮自动勾选复选框并关闭。
  - **审核状态流程**：
    - 提交后立即进入「审核中」状态（⏳ 动画图标 + 提示文字）。
    - 模拟 **3 秒审核等待**，根据联系电话判定结果：
      - `13100000001` → **审核通过** → 自动跳转进入代理商中心页面（展示推广数据 / 推广素材 / 提现申请 / 佣金明细 / 提现记录）。
      - `13100000002` → **审核驳回** → 展示驳回状态（❌ 图标 + 驳回原因卡片，含具体驳回文字说明）+ 「重新申请」按钮。
      - 其他号码默认通过（原型友好）。
    - **重新申请**：点击后清空旧状态与表单数据，回到申请表单页重新填写提交。
  - 状态持久化：`s.agentStatus`（`""`/`"pending"`/`"approved"`/`"rejected"`）、`s.agentRejectReason`、`s.agentApplyData` 存入 session。
  - 原有代理商中心功能完整保留（推广统计五卡片、推广素材+二维码、提现申请、佣金明细表格、提现记录表格、直推/间推成员弹窗）。
- **assets/css/agent.css 新建**：代理申请专用样式（表单字段、营业执照上传区、协议勾选行、审核状态展示、驳回原因卡片、协议弹窗内容、响应式适配）。

---

## 2026-08-13（迭代 63）企业提交弹窗「消费后余额」改为本次消费金额的负数

- **话费充值页（recharge.html）与企业短信群发页（sms.html）**：企业用户（余额支付路径）提交确认弹窗中「消费后余额」卡片，由原先的「真实剩余余额 `s.balance - total`」改为**本次消费金额的负数 `-total`**（按需求规格：如本次消费 ¥100，则显示 -¥100）。
- 移除提交函数中已不再使用的 `afterBalance` 临时变量。
- 个人用户（微信扫码路径）不展示余额卡片，此改动仅影响企业用户弹窗。

---

## 2026-08-13（迭代 62）短信群发支付按用户类型分流 + 账户中心按钮垂直居中修复

- **短信群发页（sms.html）支付流程改为按用户类型分流（与税费类型无关）**：与 迭代 61 话费充值逻辑对齐。新增判定 `var isBalancePay = s.type === "enterprise"`。
  - 个��用户：无论选「含税」还是「未税」，一律走**微信扫码支付**（`openWechatPay`，二维码 3s 自动成功，不扣余额）。
  - 企业用户：无论选「含税」还是「未税」，一律走**余额支付**（`openVerify` → `doVerifyPay` → `createOrder` + 扣减余额）。
  - `submitSend()`：确认弹窗「支付方式」标签与余额四卡片可见性改由 `isBalancePay` 控制；顺手修正「消费后余额」卡片（原误显 `-total`，改为真实剩余 `afterBalance = sess.balance - total`）。
  - `doConfirmPay()`：路由判断由 `state.tax === "taxed"` 改为 `s.type === "enterprise"`。
- **账户中心企业「立即充值」弹窗【我知道了】按钮垂直居中修复**：根因为 `.modal > * { padding: 0 24px }` 选择器优先级高于 `.btn`，覆盖了按钮的 9px 纵向内边距，使按钮被压成文字高度、文字贴边不居中。新增规则 `.modal > .btn { padding: 9px 18px; }`（优先级 0,2,0 > 0,1,1）恢复按钮自身内边距，文字在按钮内上下居中。
- 内联 JS 经 `new Function` 解析校验通过（sms.html / account.html / recharge.html 各 2 段）。

---

## 2026-08-13（迭代 61）订单支付流程按用户类型分流 + 账户中心个人隐藏累计充值

- **订单提交支付流程改为按用户类型分流（与税费类型无关）**：
  - 新增判定 `var isBalancePay = s.type === "enterprise";`，支付方式不再由 `state.tax` 决定。
  - **个人用户（`s.type !== "enterprise"`）**：不论选择「含税」还是「未税」，一律走**微信扫码支付**流程（`openWechatPay`，展示二维码 3s 后自动成功，不扣账户余额）。
  - **企业用户（`s.type === "enterprise"`）**：不论选择「含税」还是「未税」，一律走**余额支付**流程（`openVerify` → `doVerifyPay` → `executePayment`，扣减账户余额）。
  - `submitOrder()`：确认弹窗「支付方式」标签与余额四卡片可见性改由 `isBalancePay` 控制（个人用户不展示余额卡片）。
  - `doSubmitOrder()`：路由判断由 `state.tax === "taxed"` 改为 `s.type === "enterprise"`。
  - `executePayment()`：余额扣减条件由 `state.tax === "taxed"` 改为 `s.type === "enterprise"`。
  - 修正确认弹窗「消费后余额」卡片：原误展示 `-total`，现改为展示真实剩余余额 `afterBalance = s.balance - total`。
- **账户中心个人用户不再显示「累计充值金额」**：`#flowSummary` 个���用户汇总条仅保留「累计消费金额」一项（移除「累计充值金额」卡片）。企业用户四卡片不受影响。

---

## 2026-08-13（迭代 60）账户中心按用户类型差异化展示

- **个人用户不展示账户充值组件**：`rechEntry` 面板对 `s.type !== "enterprise"` 直接 `display:none`（含「立即充值」按钮）。
- **个人用户不展示卡片数据组件**：`#balanceTiles` 卡片网格对个人用户 `display:none`。
- **个人用户交易明细增加累计消费金额字段**：因卡片隐藏后该字段丢失，在交易明细面板顶部新增 `#flowSummary` 汇总条（仅个人用户显示），展示「累计消费金额 / 累计充值金额」（由 FLOWS 实时计算）。
- **企业用户立即充值弹窗按钮文字居中**：`.btn` 增加 `text-align:center`（与既有的 `inline-flex + justify-content:center` 互为兜底），确保弹窗「我知道了」等按钮文字居中。
- `account.html`：page-head 说明文案改为中性的「查看账户交易流水明细，掌握账户消费与充值情况。」；新增 `#rechEntry`、`#flowSummary` 容器。
- `assets/css/account.css`：新增 `.flow-summary` / `.flow-sum` 样式（含窄屏单列适配）。

---

## 2026-08-13（迭代 59）账户中心页面按需求规格全面重构

### data.js
- **FLOWS 数据结构扩展**：新增 `orderId`（关联订单号）、`account`（交易账户：系统余额/微信支付）字段；类型名对齐为「话费充值 / 短信群发 / 平台充值 / 平台扣减 / 订单退款」；扩充至 41 条演示数据（3 页分页可验证），按时间倒序排列。

### account.html
- **卡片数据**：改为 账户余额、可用预授信额度（仅企业用户显示）、累计消费金额、累计充值金额（4 列网格）；累计消费/充值由 FLOWS 实时计算。
- **列表筛选栏**：新增 4 维筛选 — 流水号/订单号关键词搜索、交易类型下拉（5 种）、交易账户下拉（2 种）、交易时间范围（起~止）。
- **交易明细表格**：列更新为 流水号 / 关联订单号（可点击跳转详情） / 交易类型 / 交易金额 / 变动后余额 / 交易账户 / 交易时间。
- **颜色规则**：扣款类（话费充值/短信群发/平台扣减）金额红色，增加/退款类（平台充值/订单退款等）绿色。
- **分页**：每页 20 条，筛选后自动重置到第 1 页并重新分页。
- **移除**：个人微信支付弹窗（`#wxMask`），简化为 toast 提示；保留企业对公收款弹窗。

### assets/css/account.css（新建）
- 账户中心专用样式：充值入口 `.rech-entry`、卡片 4 列网格 `#balanceTiles`、筛选栏 `.flow-filter-bar`、明细表格 `.flow-table`、分页 `.pagination-wrap`、对公账户 `.corp-account`；响应式适配（≤768px 筛选纵向堆叠、≤520px 隐藏关联订单号列）。

---

## 2026-08-13（迭代 58）短信模板卡片去重（单价 / 税费类型）

- **`order-detail.js` → `renderSmsDetail`**：短信模板卡片（`.sms-tpl-picked`）底部 `.sms-tpl-picked-foot` 显示的「单条价格 + 税费类型」与上方「基本信息」网格重复，予以移除；模板卡片现仅保留：模板名 + 短信签名 + 模板内容，与「短信群发 → 选择模板」列表展示一致。
- 基本信息网格已包含 单价、税费类型、总额、状态 等字段，无信息丢失。

---

## 2026-08-13（迭代 57）订单详情页样式优化（3 项）

1. **基本信息改为多列网格（撑满横向）**：弃用原单列表格（右侧空白），新增 `.detail-info-grid`（`display:grid; grid-template-columns: repeat(3,1fr); gap:1px; background:#eef0f3` 的卡片化分格），每个字段为 `.info-item`（label + value 上下排布，白底、圆角 10px）；充值单 9 个字段、短信单 8 个字段（含「号码总数」）按 3 列铺满整行，消除空旷感；H5（≤520px）降为 `repeat(2,1fr)`。
2. **卡片 PC 一行展示**：`#detailBody .tiles` 覆盖为 `grid-template-columns: repeat(5, 1fr)`，5 张统计卡片（号码总数/充值中/充值成功/充值失败/退��总额）在 PC 端单行排列；H5（≤520px）降为 `repeat(2, 1fr)` 自适应换行。
3. **明细列表加分页**：新增 `detailPageSize=20`、`applyDetailPagination()`、`goDetailPage(p)`；��选后自动重置到第 1 页并重新分页；导出仍输出全量数据（不受分页影响）。

---

## 2026-08-13（迭代 56）订单详情由弹窗改为独立页面

1. **新建 `order-detail.html`**：独立详情页，通过 URL `?id=` 参数接收订单编号，含「← 返回订单列表」链接。
2. **新建 `assets/js/order-detail.js`**：从 `orders.html` 抽出全部详情渲染逻辑（规整函数/表格构建/筛选/导出），页面加载时根据 `?id=` 自动查找订单并渲染；缺少参数或未找到时显示空态提示。
3. **`orders.html` 瘦身**：删除详情弹窗 HTML（`#detailMask`）、删除全部详情相关函数（`renderRechargeDetail/renderSmsDetail/buildXxxTable/getXxxDetailsNorm/openDetail/closeDetail/filterDetailRows/exportDetail/currentDetailOrder/Norm` 及 rechargeRowBadge/smsRowBadge/getTaxLabel/getCarrierLabel）；「查看详情」按钮改为 `goDetail(id)` 跳转 `order-detail.html?id=xxx`。
4. **`orders.css`**：新增 `.back-link`（返回链接样式）；保留全部 `.detail-*` 样式供详情页复用。

---

## 2026-08-13（迭代 55）短信订单详情模板展示对齐（短信签名 + 模板内容）

1. **data.js**：`SMS_TEMPLATES` 各模板新增 `signature`（短信签名，统一为「海拔科技」）；新增 通知类 模板「订单状态提醒」(T5)，使旧种子订单 `SMS20260726018` 引用的模板真实存在（修复此前详情内容空白）。
2. **orders.html**：短信订单详情「基本信息」中的单行「短信模板」改为「短信模板」卡片，展示方式与「短信群发 → 选择模板」列表一致——模板名 + 短信签名 + 模板内容（虚线框），并附单条价格与税费说明。
3. **orders.css**：新增 `.sms-tpl-sig` 样式（与全局 `.sms-tpl-picked / .sms-tpl-content` 配套）。

---

## 2026-08-12（迭代 54）订单详情一致性修复（充值 + 短信对齐）

**充值订单详情（早期已完成但未提交的修复）：**
1. 运营商随机展示：中国移动 / 中国联通 / 中国电信 / 中国广电（订单无 carrier 时按订单号哈希补随机）。
2. 总额按「单价 × 号码数量」计算（修正 ORD20260728001：100×1.06×50 = 5300，原错显 106；同步修正 data.js 种子）。
3. 已完成订单明细不再出现「充值中」；进行中订单至少保留 1 条「充值中」，并去除「待充值」状态（新增 getRechargeDetailsNorm 规整）。
4. 明细卡片命名改为：号码总数 / 充值中 / 充值成功 / 充值失败 / 退款总额。
5. 状态筛选项仅 充值中 / 充值成功 / 充值失败（无待充值）���

**短信订单与话费充值对齐（本次需求）：**
6. 订单管理列表新增一条「进行中」短信群发订单（SMS20260812001，含 发送中 / 发送成功 / 发送失败 三种状态）。
7. 短信详情订单状态仅 进行中 / 已完成；明细状态仅 发送中 / 发送成功 / 发送失败。
8. 进行中短信订单包含 发送中 / 发送成功 / 发送失败；已完成短信订单仅含 发送成功 / 发送失败（无发送中）—— 新增 getSmsDetailsNorm 与充值逻辑对称。
9. 短信明细卡片命名对齐：号码总数 / 发送中 / 发送成功 / 发送失败 / 退款总额（原 进行中数量 / 成功数量 / 失败数量）。
10. 短信导出数据改用规整后的明细（与充值一致）。

---

## 2026-08-13（迭代 53）订单管理页面细节优化（11 项）

**第一轮（用户截图 8 项）：**
1. 筛选组件与卡片数据之间、各区块之间统一留白间距（18px 一致节奏）。
2. 列表类型改为「话费充值 / 短信群发」。
3. 订单状态仅保留「进行中 / 已完成」两种；部分失败归入已完成。
4. 筛选状态仅「进行中 / 已完成」；提交时间改为时间段（起~止日期）选择，替代近7天等选项；筛选缓存同步改为起止日期。
5. 卡片数据去掉「部分失败」统计，保留���单数量/进行中/已完成。
6. 列表「成功/失败数」改为「成功/失败/总数」三字段。
7. 列表退款金额/支付总额不再显示负数，直接展示正数金额（如 ¥500.00 / ¥1000.00）。
8. 操作列按钮改为可见的「查看详情」按钮（原 a 标签文字不显示问题修复）。

**第二轮（用户补充 3 项）：**
9. 三张汇总卡片改为平均分布（覆盖基类 repeat(4,1fr) → repeat(3,1fr)，消除右侧留空）。
10. 列表空状态图标与文字居中（表格加 width:100% 充满面板；空态内容用 flex 居中）。
11. 重置按钮从 btn-ghost（无边框纯文字）改为 btn-outline（带边框，更像按钮）。

---

## 2026-08-13（迭代 52）订单管理页面按需求规格全面重写

1. **筛选栏重构**：去掉「时间范围」和「面值」筛选项，新增「订单编号」（文本输入）、「税费」（含税/未税）、「提交时间」筛选；保留订单类型与状态；增加「重置」按钮。
2. **汇总统计卡片**：改为「订单数量 / 进行中 / 已完成 / 部分失败」四卡片（与需求一致）。
3. **列表表格列调整**：订单编号、类型、税费、成功/失败数、退款金额/支付总额（有失败显示退款红字+支付总额灰字，无失败仅显示金额）、状态、提交时间 + 操作(查看详情)。
4. **充值订单详情弹窗**：基本信息表（订单号/类型/运营商/面额/税费类型/单价/总额/状态/提交时间）；五卡片统计（号码总数/进行中/成功/失败/退款总额）；明细支持按手机号+状态筛选；导出 CSV 功能；详情列表含手机号/状态/失败原因/回调时间。
5. **短信订单详情弹窗**：基本信息表（订单号/类型/税费类型/单价/总额/状态/提交时间/短信模板）；同上五卡片；同上筛选+导出；详情列表含手机号/状态(发送中/发送成功/发送失败)/发送结果说明/回调时间。
6. **分页**：每页 20 条记录，上一页/下一页导航。
7. **空状态**：列表为空时展示「暂无订单记录」图标提示。
8. **搜索缓存**：筛选条件存 localStorage，刷新后自动恢复并重新查询。
9. **新增 assets/css/orders.css** 订单管理专用样式（筛选栏/列表/分页/详情弹窗/响应式）。

---

## 2026-08-13（迭代 51）确认弹窗优化：去手机号明细 + 余额卡片 2×2

1. 话费充值与短信群发「确认提交订单」弹窗均不再展示具体手机号（删除 confirm-phones-section 号码 chip 列表）；号码数量已在弹窗表格内展示，满足需求。
2. 确认弹窗底部余额四卡片（账户余额/预授信额度/本次消费金额/消费后余额）由 3 个一��改为 2 个一排（2×2 网格）：recharge.css 与 sms.css 的 .balance-cards 基准与 ≤880 响应式均由 repeat(3,1fr) 改为 1fr 1fr，并移除 ≤640 时末卡跨两列的样式，保持全尺寸 2×2 一致。

---

## 2026-08-13（迭代 50）短信群发页面 4 项优化

1. 短信模板选择列表去掉「含税/未税」标签（含税未税是在外部税费类型处选择，非模板属性）。
2. 短信群发页面去掉「④ 费用统计」面板（底部统计栏已展示费用合计），并移除 update() 中对已删 estBox 的赋值，避免空引用报错。
3. 修复含税支付安全验证弹窗样式错乱：verify-code-row 闭合标签误用 `</p>`，改为正确的 `</div>`。
4. 未税支付与话费充值一致：确认支付弹出微信扫码二维码弹窗（wechatMask + buildQrSvg），3s 后提示「支付成功」自动跳转订单管理页。

---

## 2026-08-13（迭代 49）支付流程拆分：含税余额��付/未税微信扫码

1. 含税=余额支付：确认弹窗余额卡片显示 预授信额度=¥2000、消费后余额=-消费金额（负数）；金额>2000 提示「余额/预授信额度不足无法支付」拦截；≤2000 确认支付后提示「支付成功」自动跳转订单管理页。
2. 未税=微信支付：修复支付方式乱码（微信用正确字符）；确认弹窗不显示四张余额卡片；点击确认支付弹出仿真二维码，3s 后提示「支付成功」自动跳转订单管理页。
3. 新增微信扫码弹窗（wechatMask）与 buildQrSvg 仿真二维码；style.css 增加 wechat-modal/wechat-qr 样式；executePayment 改为仅含税扣减余额+自动跳转。

## 2026-08-13（迭代 48）按截图5项需求调整充值与短信页面

1. 确认弹窗余额区：3卡→4卡（账户余额/预授信额度/本次消费金额/消费后余额）
2. 含税文字修正：去掉"&"符号和"机票"错字，统一为「含税（6%专票）」
3. 未税=微信扫码支付（个人用户流程，跳过验证码直接确认）；含税=余额支付+验证码不变
4. 短信群发底栏样式对齐话费充值页（改用 recharge-summary 类 + 引入 recharge.css）

## 2026-08-13（迭代 47）充值/短信页去掉余额与预授信额度拦截

- 移除 recharge.html 与 sms.html 提交单据前的 `if (total > avail) { toast("余额/预授信额度不足"); return; }` 拦截，原型直接进入支付流程（演示账户余额充足即���）。
- 余额卡片（当前/本次消费/消费后）仍展示，afterBalance 保留 Math.max(0, ...) 防负值显示。

## 2026-08-13（迭代 46）修复登录/注册协议勾选后按钮未启用 bug

- 根因：直接点击复选框��方框时，监听对 `input` 标签提前 return，未同步按钮 `disabled` 状态，导致勾选后按钮仍置灰不可点。
- 修复：为复选框自身加 `change` 事件监听器来同步按钮启用状态；点文字区仍手动切换。登录（密码/验证码两模式）与注册（个人/企业共用同一按钮）均修复。

## 2026-08-13（迭代 45）登录与注册表单加《用户协议》《隐私政策》勾选

- 登录页（密码登录 / 验证码登录）与注册页均新增「用户协议与隐私政策」勾选区，复用同一份协议弹窗内容。
- 未勾选时提交按钮置灰（`.btn-primary:disabled`）；提交时若未勾选则拦截并提示。
- 点击《用户协议》《隐私政策》可弹出共用协议弹窗，点击遮罩或关闭按钮收起。
- 样式新增 `.agreement` 样式表（`style.css`）。

## 2026-08-13（迭代 44）短信模板去标签 + 充值/���信页统一去个人/企业区分

- data.js：4 个短信模板移除 `category: "营销类"` 字段（前端不再展示分类标签）。
- sms.html：模板列表与已选摘要移除「营销类」badge；删除顶部「个人充值」支付模式 banner；
  确认弹窗/支付执行移除 `sess.type` 个��/企业分支，统一为「余额支付 + 安全验证码」流程（前端不区分个人/企业）。
- recharge.html：删除顶部「个人充值」支付模式 banner；确认弹窗/支付执行移除 `s.type` 个人/企业分支，
  统一为「余额支付 + 安全验证码」流程（充值面额前端无感，不按账户类型区分）。

---

## 2026-08-13（迭代 43）短信群发页面按需求规格大改

- sms.html 完整重写：步骤化面板布局（税费类型→短信模板→发送号码→费用统计）
- 新增拖拽上传弹窗（支持 .xlsx/.xls 提示）、模板下载（CSV）、异常号码标红
- 新增提交按钮置灰规则（税费/模板/号码三缺一即置灰，文案动态变化）
- 订单确认弹窗增强：支付方式、手机号 chip 列表、企业三卡片余额（当前/本次消费/消费后）
- 企业验证码流程：60s 倒计时重发、演示码 111111
- 支付状态窗口：成功展示任务号 → 前往订单管理
- 个人用户=微信扫码支付；企业���户=余额支付+验证码
- 含税金额计算：单价×(1+6%)，切换税费实时更新金额
- 新增 assets/css/sms.css 短信页专用样式

## 2026-08-13（迭代 42）充值面额调整为固定 6 个选项

- data.js：FACE_VALUES 由 8 种改为固定 6 种 `[30, 50, 100, 200, 300, 500]`（国内话费充值通用标准档位）
- 充值页面值卡片、面额说明弹窗、单价计算均自动跟随同步

---

## 2026-08-13（迭代 41）话费充值页面按需求规格大改

- **新增运营商选择**：中国移动/联通/电信/广电四选一，高亮选中态，必选校验
- **扩展充值面额**：从 4 种扩展为 8 种（10/20/30/50/100/200/300/500）
- **新增充值面额说明弹窗**：展示每种面额的含税/未税单价对照表
- **重构号码导入**：新增导入弹窗（拖拽上传区 + 文件选择器），支持 .xlsx/.xls 格式提示；模板下载功能（导出 CSV 模板）；导入结果预览 + 异常号码标红
- **增强底部统计栏**：合计面额、税费面额、税费类型、单价、税额数量五项统计 + 提交单据按钮
- **增强订单确认弹窗**：展示运营商/税费类型/面值/单价金额/税额数量/支付方式/手机号码列表；企业用户显示当前余额/本次消费/消费后余额三卡片；个人用户提示微信��码支付
- **新增企业验证码验证流程**：企业用户提交订单需先输入绑定手机号验证码（演示码 111111），60s 倒计时重发
- **新增支付状态窗口**：支付成功后展示订单号 + 前往订单管理入口
- **提交按钮置灰规则**：运营商/面额/税费三项任一未选时提交按钮置灰不可点击
- **异常处理**：号码格式错误在输入区标红并计数提示；导入文件中异常号码单独列表展示
- 新增 `assets/css/recharge.css` 充值页专用样式；`data.js` 新增 CARRIERS 运营商常量

---

## 2026-08-12（迭代 40）登录页移除「一键体验登录」按钮

- login.html：删除「一键体验登录（演示账号）」按钮及其配套的「或」分隔线，移除 `quickDemo()` 函数；登录页现仅保留密码登录 / 验证码登录两种方式与底部「立即注册」入口

---

## 2026-08-12（迭代 39）注册页个人/企业注册修复（点击无反应）

- register.html：提交处理函数改用显式 `document.querySelector()` 引用控件（原 `form.this.fieldName` 在预览 iframe 中失效返回 undefined，导致 `.value` 抛异常被静默吞掉、按钮无反应），整体包 `try/catch` 兜底
- data.js：`setSession()` 加 `try/catch`（iframe 中 localStorage 可能抛错）
- 验证：jsdom 真机复现，个人 / 企业分支提交后均成功创建 `hc_session`，0 错误

---

## 2026-08-06（迭代 31）短信模板改为弹窗选择 + 全部营销短信

- sms.html：移除页面顶部直接列出的模板，改为「📋 选择短信模板」按钮；点击弹出模板选择弹窗，展示后台配置的可用营销模板（内容 + 单价，只读不可改），客户选择后回���已选摘要卡片（含「更换 ›」入口）
- data.js：SMS_TEMPLATES 全部改为「营销类」，重写文案为营销短信（会员日大促 / 周年庆狂欢 / 节日礼遇 / 专属福利券，均带「回TD退订」）
- style.css：新增 `.sms-tpl-picked` 已选模板摘要卡片样式

---

## 2026-08-06（迭代 33）移除���信模板面板提示文案

- sms.html：移除「模板由商务后台预先创建、定价完成，您仅可查看确认，不可修改单价或文案。」提示段落

---

## 2026-08-06（迭代 38）互换短信页面②导入号码与③费用预估卡片位置
- sms.html：grid-2 布局中，原左列（①+②）/ 右列（③+计费规则）调整为左列（①+③）/ 右列（②+计费规则），即②导入发送号码与③费用预估互换；计费规则跟随②

---

## 2026-08-06（迭代 37）移除已选模板摘要🔒后台定价文案
- sms.html：删除已选模板摘要 foot 中的「🔒 后台定价」标识（含多余分隔符）

---

## 2026-08-06（迭代 36）移除短信模板标题中（后台定�� · 只读）
- sms.html：标题「① 选择专属短信模板（后台定价 · 只读）」改为「① 选择专属短信模板」

---

## 2026-08-06（迭代 35）移除短信模板卡片锁标识文案
- sms.html：删除模板弹窗中每条模板卡片底部的「🔒 后台定价 · 仅可查看，不可修改」标识

---

## 2026-08-06（迭代 34）移除短信模板弹窗提示文案
- sms.html：删除模板选择弹窗中「以下为商务后台为您预创建并完成定价的专属营销短信模板……单价与文案不可修改。」提示段

---

## 2026-08-06（迭�� 32）移除短信群发页面功能说明 banner

- sms.html：移除顶部「📨 功能说明」banner 区域

---

- **导航**：`app.js` NAV_ITEMS 与 `index.html` 子导航均在「话费充值」与「订单管理」之间加入「短信群发」入口
- **新建 `sms.html`**（覆盖需求 1–9）：
  1. 功能说明 banner：系统仅做号码上传/提交发送/费用预扣/失败退费；短信模板与签名审核由短信服务商完成，需单独对接发送接口
  2. 专属模板列表（后台定价，前端只读，带 🔒 不可修改标识）
  3. 前端查看后台预创建+定价的模板，确认文案与单价
  4. 批量粘贴 / Excel 导入号码
  5. 选定模板 + 号码��提交发送
  6. 提交前短信验证码校验（确认本人操作，原型展示演示验证码）
  7. 预扣费用 + 按第三方回执成功条数结算 + 失败条数自动退费（页面与确认弹窗均有说明）
  8. （见 orders.html）提交后任务进入「订单管理」可查进度
  9. 含税 / 未税两种计价模式（与充话费一致，含税 ×1.06）
- **`assets/js/data.js`**：新增 `SMS_TEMPLATES`（4 个后台模板）；新增短信示例任务 `SMS20260726018`（部分失败、含退款明细）进入 `ORDERS`
- **`orders.html`（需求 8）**：新增「订单类型」筛选与「类型」列；SMS 任务显示整体进度（发送中/成功/失败）+ 单条回执 + 退款明细；移动端列隐藏索引同步调整
- **`assets/css/style.css`**：新增 `.sms-tpl*` / `.code-input` / `.sms-rules` / `.sms-detail-table` 等样式

---

## 2026-07-29（迭代 29）

- **修复账户中心交易明细表格显示异常**：桌面端 6 列表格被截断，仅显示前 4 列且底部出现异常滚动条。
  - **根因**：交易明细被包裹在 `.grid-side`（`grid-template-columns: 320px 1fr` 两列布局）的左侧 **320px 窄列**中（原用于左放退款说明、右放交易明细），6 列表格在 320px 内完全放不下。
  - **修复**：移除 `.grid-side` 包裹（退款说明已在迭代 27 删除，无需两列布局），交易明细面板直接占满容器宽度；去掉面板 `overflow:hidden`。

---

## 2026-07-29（���代 28）

- **订单列表「查看明细」按钮移到最左列**：
  - `orders.html` 表格第 1 列改为操作列（放置「查看明细」按钮），订单号/面值/税费/号码数/总额/状态/提交时间依次右移；表头首列置空。
  - `style.css` 移动端隐藏列索引同步调整：≤640px 隐藏税费（现第 4 列）与提交时间（现第 8 列），≤480px 隐藏面值（现第 3 列）；第 1 列「查看明细」始终保留，不被隐藏。

---

## 2026-07-29（迭代 27）

- **账户中心移除退款说���区域**：删除「退款说明」面板（含「账户余额仅可用于充值消费」「退款需人工沟通办理」「客服联系方式」三段文字），页面仅保留交易明细。

---

## 2026-07-29（迭代 26）

- **修复提现记录状态文字**：提现记录中「已到账」状态显示为「充值成功」（复用了充值订单的 `statusBadge`），改为直接渲染正确的状态文字——已到账显示「提现成功」，其他显示「审核中」。

---

## 2026-07-29（迭代 25）

- **代理商中心体现直推/间推佣金规则（仅企业有佣金）**：
  - `data.js` 新增 `AGENT_COMMISSION`（直推 3‰ / 间推 2‰）常量；`AGENT_DIRECT`/`AGENT_INDIRECT` 成员加 `recharge` 字段。
  - 佣金明细改为按规则生成：仅类型=企业的下线产生佣金，个人下线不产生（原型当前直推企业 2 人 / 间推企业 2 人）。
  - `agentStats()` 新增 `directComm`/`indirectComm`/`directEnt`/`indirectEnt` 拆分返回。
  - `agent.html` 佣金明细区新增规则说明（直推 3‰、间推 2‰，仅企业用户产生佣金，个人无佣金）+ 三枚汇总标签（直推佣金¥X 企业直推 Y 人 / 间推佣金¥Z 企业间推 W 人 / 个人推广 0 佣金）；表格列由「关联订单」改为「来源成员」并新增「成员类型」列。
  - 直推/间推成员弹窗新增「佣金」列：企业成员显示金额、个人成员显示「无佣金」。
  - `style.css` 新增 `.comm-pill`（汇总标签）/`.muted`（无佣金灰字）；佣金表 ≤640px 隐藏「成员类型」「产生时间」。

---

## 2026-07-29（迭代 24）

- **账户中心退款说明 / 交易明细移动端溢出修复**：
  - 退款说明区域：banner 和提示文字加 `word-break:break-word`，长句自动换行不再被截断。
  - 交易明细表格：外层增加 `overflow-x:auto` 容器；新增 `.flow-table` 类名用于精确选择。
  - ≤640px 隐藏「操作人」和「时间」两列，保留核心 4 列（流水号 / 类型 / 金额 / 变动后余额）。

---

## 2026-07-29（迭代 23）

- **代理商中心佣金明细 / 提现记录移动端表格溢出修复**：手机端两张表格右侧列（「产生时间」「时间」）被截断看不到。
  - 两张表格外层增加 `overflow-x:auto` 容器（原 `.panel` 的 `overflow:hidden` 会截断内容）。
  - 表格新增 `.comm-table`/`.wd-table` 类名用于精确 CSS 选择。
  - ≤640px 媒体查询隐藏佣金明细「产生时间」列和提现记录「时间」列，保留核心 4 列完整显示。

---

## 2026-07-29（迭代 22）

- **代理商中心新增直推/间推人数及成员明细**：
  - `data.js` 新增 `AGENT_DIRECT`（直推 7 人）/`AGENT_INDIRECT`（间推 8 人）成员数据集，含昵称、账号、类型、手机号、注册时间（间推额外含上级直推人字段 `via`）；`agentStats()` 返回 `direct`/`indirect` 人数。
  - `agent.html`：原「累计推广客户数」卡片拆为两张可点击卡片「直推人数」（蓝）「间推人数」（绿），点击弹出成员明细弹窗；直推弹窗列（昵称/账号/类型/手机号/注册时间），间推弹窗额外多一列「上级（直推）」。
  - `style.css`：新增 `.tile.clickable` 可点击卡片样式（hover 上浮 + 蓝色描边 + 标签后「查看 ›」提示）。

---

## 2026-07-29（迭代 21）

- **充值页面增加个人/公司充值模式提示卡片**：在话费充值页面（`recharge.html`）表单顶部新增 `.pay-mode-banner` 支��模式提示区域，根据当前登录用户类型动态展示：
  - **个人用户**：显示 👤 图标 +「个人充值」标题 + 微信支付说明 + 蓝色「个人用户」标签
  - **企业用户**：显示 🏢 图标 +「公司充值」标题 + 余额/预授权扣款说明 + 绿色「企业用户」标签
  - 新增 CSS 样式（`.pay-mode-banner` / `.pay-mode-icon` / `.pay-mode-info` / `.pay-mode-badge`），含移动端响应式适配（≤640px 居中纵向排列）

---

## 2026-07-29��迭代 16）

- **账户中心新增「账户充值」入口**：位于标题下方、余额卡片上方。
  - 个人用户：仅手机端（UA 或 ≤640px）时弹「微信支付充值」弹窗（金额 chips + 自定义输入 + 调起微信支付，原型模拟加款）；非手机端提示「请在手机端使用微信支付」。
  - 企业用户：弹「对公账户充值」弹窗，展示收款���名/开户银行/收款账号（含备注与客服提示）。
  - `data.js` 新增 `CORP_ACCOUNT`（对公账户）、`RECHARGE_AMOUNTS`（个人可选金额）。
  - 新增样式：`.rech-entry`、`.amount-chips`/`.amount-chip`、`.corp-account`/`.ca-row` 等。

---

## 2026-07-29（迭代 17）

- **修复所有弹窗文字被遮挡问题**：重构 `.modal` 布局方式。
  - **根因**：之前 `.modal h3` 使用 `margin: -24px -24px 16px` 负 margin 拉伸到弹窗边缘，导致标题栏与下方内容区域重叠，说明文字和底部内容被裁切。
  - **修复**：`.modal` 改为 `display: flex; flex-direction: column; padding: 0`；h3 用独立 `padding: 18px 24px` + `position: sticky; top: 0` 吸顶（不再用负 margin）；其余子元素通过 `.modal > * { padding: 0 24px }` 统一左右内边距；最后一个子元素自动加 `padding-bottom: 24px`。
  - 所有弹窗统一生效：确认提交、成功提示、订单明细、微信支付、对公账户。

---

## 2026-07-29（迭代 18）

- **修复订单管理页移动端「查看明细」��钮被遮挡**：
  - ≤640px 已隐藏「税费」「提交时间」2 列，但剩余 6 列在手机宽度下按钮仍被截断。
  - **新增 ≤480px 断点**：再隐藏「面值」列（可由总额÷数量推算），保留 5 列（订单号/号码数/总额/状态/操作）；单元格 padding 缩至 8×6px，字号 12px；`.btn-detail` 按钮缩小为 `padding:6px 10px`。
  - 表格容器加 `-webkit-overflow-scrolling:touch`（iOS 平滑滚动）；表格设 `min-width:560px` 确保内容不被压缩。

---

## 2026-07-29（迭代 19）

- **修复弹窗移动端两侧无安全距离**：`.modal-mask` 的 `padding` 从固定 `16px` 提��为 `max(20px, env(safe-area-inset-*))`，四边统一加大安全间距，兼容刘海/圆角屏。

---

## 2026-07-29（迭代 20）

- **修复退出登录按钮点击无效**：
  - **原因排查**：退出按钮使用 `onclick="App.logoutAndGo()"` 内联绑定，在某些环境下可能因 `confirm()` 被拦截/返回异常值导致函数提前 return，或 innerHTML 生成的 onclick 绑定不可靠。
  - **修复**：
    1. 退出按钮改为 `id="btnLogout"`，通过 `addEventListener("click", logoutAndGo)` 在 DOM 插入后显式绑定事件（受保护页面 + 首页同步修改）。
    2. `confirm()` 调用加 `try-catch` 保护：若 confirm 被禁用/抛异常，默认继续执行退出（不卡死）。

---

## 2026-07-29（迭代 15）

- **弹窗滚动后关闭按钮不可点击修复**：订单明细等长内容弹窗，向下滚动后顶部 × 按钮滚出视口无法关闭。
  - `.modal h3` 改�� `position: sticky; top: 0` 吸顶固定，白色背景 + 底部阴影分隔线，负 margin 抵消弹窗 padding 使标题栏贴顶。
  - 所有 `.modal` ��窗（确认提交、成功提示、订单明细）统一生效。

---

## 2026-07-29（迭代 14）

- **订单管理页移动端表格溢出修��**：8 列表格（订单号/面值/税费/号码数/总额/状态/时间/操作）在手机端右侧被截断。
  - 表格容器从 `overflow:hidden` 改为 `overflow-x:auto`（允许横向滚动）。
  - ≤640px 隐藏「税费」和「提交时间」两列次要信息，保留核心 6 列；单元格内边距缩小、字号 13px。
  - 明细弹窗内表格 ≤520px 隐藏「回调时间」列。

---

## 2026-07-29（迭代 13）

- **确认弹窗余额卡片移动端截断修复**：去掉内联 `repeat(3,1fr)` 强制 3 列和内联样式，改用 `.confirm-tiles` CSS 类；≤480px 自动变为 2 列布局（第 3 张「消费后余额」横跨整行居中显示）；弹窗在 ≤560px 内边距从 24px 缩至 16px、宽度 `calc(100%-24px)` 确保不贴边。

---

## 2026-07-28（��代 12）

- **移动端横向溢出全面修复（充值页为主）**：
  - `body` 加 `overflow-x:hidden`，彻底阻止任何子元素撑开整页横滚。
  - `recharge.html` 费用预估 tiles：去掉内联 `grid-template-columns:repeat(3,1fr)`，交由全局响应式规则控制（880px→2列）。
  - `orders.html` 订单明细 tiles：去掉内联 `repeat(4,1fr)`，同上。
  - `.summary` 提交栏：880px 断点加 `flex-wrap:wrap` + 按钮字号缩小，防止两按钮并排溢出。
  - `input/textarea` 加 `overflow-wrap:break-word;word-break:break-all`，长号码不再撑开输入框。

---

## 2026-07-28（迭代 11）

- **子导航登录门控**：未登录时隐藏移动端图标+文字子导航条（首页/充值/订单等入口），避免游客看到无意义的导航。
  - `index.html`：静态 `.subnav` 默认 `display:none`，登录态脚本中 `subnav.style.display=""` 恢复显示。
  - `app.js` `renderTopbar()`：仅在 session 存在时注入 `.subnav` 元素。

---

## 2026-07-28（迭代 10）

- **统一导航文字标签**：移动端子导航文字（充值/订单/账户/代理）与桌面顶栏（话费充值/订单管理/账户中心/代理商中心）不一致，改为全部使用全称，保持两端一致；标签字号 11→12px 提升可读性。

---

## 2026-07-28（迭代 9）

- **移动端子导航加文字标签**：纯 emoji 图标用户看不懂，改为「图标 + 文字」纵向排列（icon 21px / label 11px），5 个入口均等分布（`justify-content:space-around`），active 高亮背景+文字���。
  - `app.js` `renderTopbar()`：`.icon-nav-item` 内部拆为 `.icon-nav-icon` + `.icon-nav-label` 两层。
  - `style.css`：`.icon-nav-item` 改 `flex-direction:column;gap:3px`，新增 `.icon-nav-icon`/`.icon-nav-label` 样式。
  - `index.html` 静态子导航同步更新。

---

## 2026-07-28（迭代 8）

- **移动端两侧安全距离全面修复**：
  - 关键 bug：`index/充值/订单/账户/代理商` 内页用 `<div class="container page">`，但 `.page{ padding:28px 0 56px }` 用 padding shorthand 覆盖了 `.container{ padding:0 16px }` 的左右内边距（同特异性后者胜），导致移动端内容**贴着屏幕左右边缘（0 间距）**。改为 `.page` 只设 `padding-top/bottom`，左右安全距离交由 `.container` 提供。
  - `.container`、`.topbar .inner`、`.subnav`、`.footer` 的左右内边距改用 `max(16px, env(safe-area-inset-left/right))`，兼顾刘海/圆角屏安全区（默认回退 16px）。
  - `.footer` 原无横向内边距，补 `padding:28px 16px` + 安全区。

---

## 2026-07-28（迭代 7）

- **图标导航下移 + 移除底部 Tab 栏**：上一轮把图标放���顶栏内联，本轮按需求改到「顶栏正下方」单独一行子导航条（`.subnav`，移动端 `position:sticky;top:60px` 跟随吸顶）。
  - `app.js`：`renderTopbar()` 将图标行从顶栏 `.inner` 内移到独立 `.subnav` 元素，插入到顶栏之后；移除��部 Tab 栏注入与 `has-tabbar`，删除未用的 `TAB_ICONS` 常量。
  - `style.css`：删除 `.hamburger` / `.mmenu-*` / `.tabbar` / `body.has-tabbar` 全部失效样式；新增 `.subnav` 容器样式。
  - `index.html`：首页静态顶栏同步改为顶栏下方独立 `.subnav` 图标条。
  - 移��端仅保留「顶栏下方图标子导航」一处入口，避免与底部 Tab 栏重复。

---

## 2026-07-28（迭代 6）

- **移动端导航改造**：去掉汉堡按钮 + 全屏侧滑面板，改为顶栏直接显示一排图标导航（🏠首页 / 📱充值 / 📋订单 / 💰账户 / 🤝代理），点击即跳转，无需额外展开操作。
  - `assets/js/app.js`：`renderTopbar()` 移除汉堡按钮和 mmenu 遮罩/面板注入，新增 `.icon-nav` 内联图标行；删除 `openMmenu()`/`closeMmenu()` 函数。
  - `assets/css/style.css`：新增 `.icon-nav`/`.icon-nav-item` 样式（38×36px 圆角图标按钮，active 高亮）；移动端媒体查询改为显示 `.icon-nav` 而非 `.hamburger`。
  - `index.html`：首页顶栏同步加入 `.icon-nav` 图标导航，移动端与功能页一致。

---

## 2026-07-28（迭代 5）

- **充值确认弹窗溢出修复**：提交订单的二次确认弹窗在部分屏幕下内容（表格 + 余额三卡片）横向溢出模态框边界。
  - `.modal` 全局样式：`overflow: auto` 拆分为 `overflow-y:auto; overflow-x:hidden`，��加 `box-sizing:border-box`，彻底阻��水平溢出。
  - 确认弹窗内表格加 `table-layout:fixed;width:100%` 固定列宽不撑开。
  - 企业用户三张余额卡片缩小内边距（16→12px）、字号（24→20px）、间距（14→8px），确保在 480px 弹窗内不换行溢出。

---

## 2026-07-28（迭代 4）

- **首页登录态展示**：处于登录状态时，首页右上角在「进入系统 / 退出」前新增当前登录账号展示（👤 名称 + 类型标签「企业用户 / 个人用户 · 代理商」），与受保护页顶部一致；移动端（≤880px）隐藏账号文字以保持顶部精简。未登录时仍显示「登录 / 注册」。

---

## 2026-07-28（迭代 3）

- **二次确认审计（全系统）**：排查所有破坏性 / 不可逆 / 涉及资金的敏感操作，统一补加二次确认。
  - `recharge.html`：`clearPhones()`（清空已导入号码，破坏性）→ 加 `confirm()`，提示将清空多少个号码且不可撤销。
  - `agent.html`：`applyAgent()`（申请成为代理商，不可逆）→ 已加 `confirm()`；`applyWithdraw()`（提交提现申请，涉资���）→ 已加 `confirm()`，展示提现金额与收款信息。
  - `assets/js/app.js`：`logoutAndGo()`（退出登录，清空登录态）→ 加 `confirm()`；`logout()` 仅内部调用保持不变。
  - 已有：`recharge.html` 提交订单（`submitOrder()`）走确认弹窗展示明细/余额（上一轮已加）。

## 2026-07-28（迭代 2）

- **首页优惠活动卡片**：4 张活动卡片的描述文字和底部数字全部去掉具体折扣（¥5 / 100-3 / 95折 / 98折），统一改为「有折扣」，不写具体数值。

## 2026-07-28（初始版本 + 多轮迭代）

- **初始**：根据《话费代充收单系统需求规格文档（SRS）》生成用户端多文件可交互 HTML 原型
  - 页面：index（首页，直接访问）、login（统一登录）、register（个人/企业注册）、recharge（充值）、orders（订单）、account（账户中心）、agent（代理商中心）
  - 共享：`assets/css/style.css`、`assets/js/data.js`（模拟数据）、`assets/js/app.js`（导航/登录态/工具）
  - 关键约束：首页直接进入；登录仅一种类型（个人/企业/代理商共用账号）；代理商非独立身份，个人/企业可申请升级
- **移动端导航**：顶部横向导航在手机端改为「汉堡菜单（全屏侧滑）+ 底部 Tab 栏」，解决菜单点不到的问题
- **品牌**：系统名改为「话费代充系统」，LOGO 由"海"字换成电话听筒图标
- **首页改版**：改为纯宣传落地页（秒充秒到账 / 充值有折扣等虚构营销文案），去除代理商、佣金、分销相关内容
- **订单状态**：订单（批次）层级「充值失败」改为「部分失败」；单号码明细仍保留「充值失败」
- **订单列表**：「查看明细」由文字链接强化为醒目的蓝色实心按钮
- **账户中心**：移除「提交退款申请」在线表单，改为「退款需人工沟通办理」说明（退款为人工沟通，无在线申请）
- **代理商中心**：提现申请表单新增「户名」字段，提交校验同步更新
- **首页文案**：去除「新人首充立减 ¥5」飘带；数据条折扣数字「低至 9.2 折」改为「有折扣」
